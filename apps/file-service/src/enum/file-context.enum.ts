
export enum FilePurpose {
    PROFILE = 'profile',
    POST = 'post',
    COMMENT = 'comment',
    EDITOR = 'editor',
    ETC = 'etc',
}

export enum FileAccessLevel {
    PUBLIC = 'public',
    PRIVATE = 'private',
    SIGNED = 'signed',
}

